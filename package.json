{
  "name": "daylist",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "dev": "python3 -m http.server 5173",
    "test:e2e": "npx --yes playwright install --with-deps && npx --yes @playwright/test test"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "vitest": "^1.6.0"
  }
}
